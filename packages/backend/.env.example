# ==============================================================================
# Arquivo .env.example para o backend - Lino's Panificadora
# ==============================================================================
# IMPORTANTE: Copie este arquivo para .env e configure os valores reais.
# NUNCA commite o arquivo .env com credenciais reais!
# ==============================================================================

# ------------------------------------------------------------------------------
# Banco de Dados
# ------------------------------------------------------------------------------
DATABASE_URL="file:./prisma/dev.db"

# ------------------------------------------------------------------------------
# Ambiente
# ------------------------------------------------------------------------------
NODE_ENV="development"
PORT=3001

# ------------------------------------------------------------------------------
# Autenticação JWT (OBRIGATÓRIO)
# ------------------------------------------------------------------------------
# Gere uma chave segura com: openssl rand -base64 32
JWT_SECRET="CHANGE_ME_IN_ENV"
JWT_EXPIRES_IN="8h"

# ------------------------------------------------------------------------------
# Usuários do Sistema (para seed)
# ------------------------------------------------------------------------------
# Se não definidos, o seed não criará os usuários automaticamente
ADMIN_EMAIL="admin@linos.com"
ADMIN_PASSWORD="admin123dev"
OPERADOR_EMAIL="operador@linos.com"
OPERADOR_PASSWORD="operador123dev"

# ------------------------------------------------------------------------------
# Supabase (opcional - para storage de PDFs na nuvem)
# ------------------------------------------------------------------------------
SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="YOUR_SERVICE_ROLE_KEY_HERE"
SUPABASE_BUCKET="pedidos-pdfs"

# ------------------------------------------------------------------------------
# Caminhos de Storage (opcional - padrões funcionam para dev)
# ------------------------------------------------------------------------------
# PDF_STORAGE_PATH="/caminho/para/pdfs"
# UPLOADS_PATH="/caminho/para/uploads"

# ------------------------------------------------------------------------------
# Email (Resend)
# ------------------------------------------------------------------------------
# Chave da API do Resend (obrigatória em produção)
RESEND_API_KEY="re_YOUR_API_KEY_HERE"
# Remetente dos emails (deve ser verificado no Resend para produção)
EMAIL_FROM="Lino's Panificadora <noreply@resend.dev>"
# URL do frontend para links nos emails (ex: reset de senha)
FRONTEND_URL="http://localhost:3000"
# Se 'true', emails são logados no console em vez de enviados (dev/testes)
EMAIL_MOCK="true"

# ------------------------------------------------------------------------------
# CORS (opcional - para produção)
# ------------------------------------------------------------------------------
# CORS_ORIGINS="https://seu-frontend.com,https://outro-dominio.com"
